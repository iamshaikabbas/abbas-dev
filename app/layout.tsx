
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shaik Abbas | Data Analytics & Software Development",
  description:
    "Portfolio of Shaik Abbas showcasing Data Analytics, SQL, Power BI, Machine Learning, and Software Development projects.",
  keywords: [
    "Shaik Abbas",
    "Data Analyst",
    "Business Analyst",
    "Software Developer",
    "Machine Learning",
    "SQL",
    "Power BI",
    "Python",
    "Portfolio",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
      </body>
    </html>
  );
}

